// The function should accept array of CSS class names
// and join it in a single string, splitted by space.
export function joinClassNames <T>(classNames: T[]): string {
    // @TODO: implement me

    return classNames.join(" ");
}
